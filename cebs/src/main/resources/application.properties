spring.application.name=cebs

# Database Configuration (Supabase/PostgreSQL)
spring.datasource.url=jdbc:postgresql://postgres:[YOUR-PASSWORD]@db.swwwqsrutduqgltphvnw.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=@g05a12v22e
# driver-class-name is optional when using Spring Boot 3 with the Postgres driver on the classpath

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=mySecretKeyThatIsAtLeast32CharactersLongForHS256Algorithm
jwt.expiration=86400000

# Server Configuration
server.port=8080
server.servlet.context-path=/
